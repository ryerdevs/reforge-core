---
Type: Snapshot
Status: Current
Audience: Contributors, maintainers
Last verified: 2026-08-15
---

# CURRENT — State of the project (snapshot, 2026-08-15)

This is a point-in-time snapshot. It is the **source of truth for current status**; refresh it at the end of every session that changes the state it describes. Historical narrative lives in [`../CHANGELOG.md`](../CHANGELOG.md); the full plan lives in [`../ROADMAP.md`](../ROADMAP.md).

## Snapshot

- **Date:** 2026-08-15
- **Commit:** `9a0b618` (50th part — C29-C32; working tree clean at snapshot time; 652 tests / 0 failed).
- **Rust rewrite workspace:** `source/reforge/` — crates `protocol`, `network`, `database`, `game_core` (renamed from `realm` 2026-08-13, 42nd part), **`mysql_proxy`** (temporary G-PG adapter, removed at F6), **`locale_import`** (F1 locale importer, ADR-0009), `quest_dsl`, `bench_bot` + binary `server_realms` (flat layout, ADR-0004). Rust toolchain 1.97.0, edition 2024, `unsafe_code = "forbid"`.
- **Waves 6–7 (42nd–43rd parts, 2026-08-13):** crate `realm` → `game_core` (78 refs; workspace **512 passed**); channel.rs split → `server_realms/src/channel/` (13 files, `Session` + `Outcome`; LoginGuard lifetime bug found+fixed); ecs.rs split → `game_core/src/ecs/` (`Intent`/`NpcEvent` wrapper sub-enums; N1 trap guards `systems/{social,quest}.rs`); PG migrations `scripts/gpg/alter_gold_check.sql` (CHECK gold>=0, money_log excluded) + `migrate_guild_tables.sql`; quest similarity engine (`quest_dsl` 66 tests). **NPC shops + player trade** (`game_core/src/shop.rs` 406 lines + `trade.rs` 370 lines — one-tx commit via `exchange_mutated` + `Batcher::flush()`, ids 100M–200M; GC_SHOP 1888 B / GC_EXCHANGE 47 B; workspace 548 passed); **quest runtime engine** (`game_core/src/quest/engine.rs` 859 lines — `{quest}.__status` 1-based, wait/select suspension, `player.quest` persistence; GC_SCRIPT 45 / CG_SCRIPT_ANSWER 29; workspace 564 passed; pending actions: say_reward, send_letter, set_quest_state, target_vid, affect_*, input_number); **GM subset** (`gm.rs` + `channel/gm.rs` — warp/item/notice/level; mob spawn + locale pending); **dw_arrow** (arrow gate `USE_ARROW_DAMAGE` + `consume_arrow` + real arrow count in GC_CHARACTER_ADDITIONAL_INFO — packets.rs:311-333; cargo check 4.52 s, game_core 154 + server_realms 42 passed); **background subagents enabled** (`OPENCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS=true` — opencode restart needed). **Deploy: waves 6–7 shipped in the wave-44 binary — deploy DONE + verified 18:01:39 (see the wave-44 bullet below); the earlier staging note is superseded.**
- **WAVE 44 (2026-08-13, 44th part): pool + shared Batcher + metrics + ladder step 1** — `deadpool-postgres 0.14.1` (same driver: tokio-postgres 0.7.9 — no upgrade); NEW `database/src/pool.rs` (`PgPool`); ~13 `database/src` repos → `pool.get()`; `PgMutationSink::new(pool)` (wal.rs); `WorldStore::new(pool, Arc<Batcher>)` — **ONE `Batcher` per channel** (was per player); `pool_max_size` default 10 (config.rs:110); `channel/shop.rs` direct SQL → `ItemRepo::load_sell_proto` (ADR-0008 §2 restored); `WorldMetrics.last_tick_ms` + `record_metrics` → `tick_ms.csv`; `replay_wal` fail-open at boot. Workspace **565 passed / 0 failed / 35 ignored**; clippy identical to baseline; release built 17:57. **Deploy verified 18:01:39** — binary 4,509,184 B, SHA256 `77D8ACD2…C732`; `start_win.ps1 -BenchCapture <rel>`. **Bench 20 bots** (vs the 06:11 baseline): world_ms median **1,052** (7,829), **spawns 11/11 on ALL 20** (baseline 19/20 with 0) — oracle criteria PASS. **Bench 100 bots (ladder step 1)**: 100/100 OK, world_ms median **2,742**, spawns 1,100 = 11/bot, AI-tick window <1 ms — <500 ms criterion PASS. Report: `source/deploy/win/logs/bench-run-2026-08-13-wave44.md`.
- **WAVE 45 (NIGHT, 2026-08-13, 45th part): real-client session COMPLETE + content gaps** — fixes: **`subtype`** (item.rs:179 — legacy `sub_type` vs PG `subtype` → 42703 ejected players with equipped items; deploy 19:38 `1B4420E2`); **walkability re-scoped** (the `server_attr` parse was CORRECT — village source cells are legitimate ATTR_BLOCK; the pre-move gate froze the player → anti-teleport reinforcement: normal steps accepted, jumps onto blocked terrain rejected; deploy 20:38 `271EDEBE`); **envelope Δt = max(client clock, server clock)** (client sends MOVEs in bursts; deploy 21:40 `19DD1724`) + **tuning 1.2→1.5 / 100→250 ms** (real pattern fits with margin; anti-cheat intact: 2500 cap + C++ timers; deploy 22:06 **`2A71E57E` — current stack auth.220608/channel.220608**). **Verified live**: entry → cross-map movement (~9k units) → dynamic spawns around the player (tanacas at the village, Uriel/Capitán at the second plaza); wave-44 benchmarks re-confirmed in vivo. **Spawns reverted to the ORIGINAL data (user directive)** — village identical (5001×1 + 5004×10 + 20354); **rule: no game-data edits without explicit user confirmation**. **Gaps — partially closed in the night follow-up**: shops LINKED to visible NPCs (`player.shop` 1–6 → 20002/20006/20023/20025/20029/20030; 7/8 gold_bar/firework unlinked); quests WIRED (corpus loads at channel boot — 194/194 convert, 106 with Chat triggers → GC_SCRIPT dialogs via `QuestIntent::NpcClick`); remaining: handshake `LoginEarly` IN PROGRESS (`MT2_HS_DEBUG=1`, network/src/handshake.rs:306-309), converter completion (~88 quests), skills GAPs, GM mob spawn. **Night follow-up (deploys 23:31 `FE6BDE06` + 00:15 `8C335FAA` — current stack 4,941,312 B, channel 001531)**: tanacas removed from the village (user directive — village `world.spawns` = only 20354; no game-data edits without explicit confirmation); vision `SPAWN_VIEW` 5000→8000 / `DESPAWN_RADIUS` 7000→10000 (spawn.rs:24,28); movement `DEFAULT_MOVE_SPEED` 300→500 + `MAX_DIST_NO_RIDING` 4000→6000 (movement.rs:81,94); mob move duration real (`move_duration_ms` ai.rs:41 — parity char.cpp:2765-2768); dot-source `start_win.ps1`. **572 tests passing** (615 attributes).
- **50th part (2026-08-15): mob combat parity C29-C32** — mob attack cooldown
  (`CalculateDuration(ATT_SPEED, 2000)` — was 250 ms per tick, `10d5092`); motion-based
  speed (`motion 300 × factor` — the 308 mobs with factor 0/1 no longer frozen, `9e55397`);
  exact range (`range×1.15` no floor + BOW_DISTANCE for RANGE/MAGIC, `3e05d3c`); change
  attack position (lateral reposition every 10 s/1 s — mobs stop clumping, `9a0b618`).
  Workspace **652 passed / 0 failed**; deployed (binary 5,297,664 B, hash 357d8620; stack
  up auth :30001 / channel :30003). Full server-side gap analysis written
  (`docs/plans/server-side-gap-2026-08-15.md`) — 28 ranked missing behaviors; CRITICAL:
  refine, stat_points (no handler — progression blocked), party, PvP; HIGH: safebox,
  numeric buffs, skill families, gold pickup, exp level-delta, item attributes, mob
  AIFLAGs, death penalty. **NEXT block: stat points → party → refine → gold pickup →
  numeric buffs → safebox → exp curve → AIFLAGs.**
- **49th part (2026-08-15): base jugable fixes + full legacy-mob analysis** — revive-city (C26: village teleport + HP/MP, `4f00706`), boots speed (C27: `APPLY_MOV_SPEED` wired to the envelope, capped 200, `41a244f`), mobs separation (C28: per-map snapshot + SEP_MOBS 60 u + spawn jitter, `261d0c7`), mobs respawn (C23: `time==0` never respawns, top-up = max_count−alive, `41a244f`+`261d0c7`). **Verifier mobs v2 PASS** (3 mutation tests, 3× 647 passed / 0 failed). Redeployed — server running (auth :30001 + channel :30003). **NEW `docs/plans/mob-legacy-behavior.md`** (`841101a`): the complete C++ mob AI captured (state machine, attack cooldown ~2 s vs 250 ms — C29, motion-based speed ~300 u/s vs column — C30, exact range ×1.15/×0.9 — C31, change-attack-position that prevents clumping — C32; **the "knockdown" does NOT exist server-side** — AIFLAG_FALL dead code, client animation only). 21 missing behaviors registered (C29–C32 in bug-registry). Backlog committed (`d2c0831` — 59 files from waves 44–46 that were documented but never committed). **NEXT (block): C29 cooldown + C30 speed + C31 range + C32 change-attack-position.**
- **WAVE 46 (DAWN, 2026-08-14, 46th part): quest-boot A/B fix + handshake leniency + panic fix** — **(1)** post-quests "no connect" root cause ISOLATED with A/B: the corpus load (194 files, ~2 s) ran synchronously before the accept loop → client hit the kernel backlog and closed; **fix: background load** (`tokio::spawn`, channel/mod.rs:132-147 — accept instant, quests ~2 s later, fail-open). **(2)** handshake leniency: after 32 retries (~17.6 s) the channel NO LONGER closes — sends `GC_PHASE(LOGIN)` and waits for LOGIN3 up to 45 s (test `channel_slow_client_login3_after_retries_exhausted`). **(3)** close panic fix: `session.rs save()` expected `motion()` on pre-ENTERGAME closed connections → sync only when motion is set (session.rs:449-457). **(4)** channel.inf: saved index "1" (4-channel era) vs 1-channel dict → KeyError → corrected to 0 (client fallback finding — pack). **(5)** shops data linked (`player.shop` 1–6 → 20002/20006/20023/20025/20029/20030, 9 → 20042, 1002 → 20341, 1003 → 20343); **shop opening IN DIAGNOSIS (coder)**. **(6)** quest dialog sends raw keys — **text resolution IN DIAGNOSIS (coder)**. **Verified live** (stack 02:11): stable entry, position persisted across reconnects, 331 movement lines, 194 quests in background, 0 panics. Deploys 01:50 `941F1B54` / 02:05 `2E9CC162` / **02:11 `015FB634` — current stack auth.021225/channel.021225**; **573 tests** (616 attrs); ops dot-source + `-HsDebug`.
- **G-PG CUTOVER COMPLETE (2026-08-10, loop):** PostgreSQL 18.4 is the single canonical store; the C++ baseline operates on it through `mysql_proxy` — real client login `test`/`1234` verified (character select reading from PG). MariaDB frozen as migration source (archived + stopped 2026-08-12). **The canonical PG is now native Windows (ADR-0012, service `postgresql-metin2` on 127.0.0.1:5432).** The C++ baseline remains the oracle — `source/reforge/**` is owned exclusively by the rewrite (ADR-0003).
- **Runtime state (ADR-0012, executed 2026-08-12):** **all-Windows native** — PostgreSQL 18.4 native (service `postgresql-metin2`, 127.0.0.1:5432, db `metin2`, role mt2/mt2, `LC_COLLATE='C'`) + **`server_realms` auth :30001 / channel :30003** native from `source/deploy/win` (`auth.toml`/`channel.toml`; `timeout_ms 120000`); the client connects to 127.0.0.1 (serverinfo.py repacked). MariaDB archived (`metin2-extra/archive/mariadb_full_2026-08-12.sql`, 5.7 MB) + stopped. WSL = on-demand oracle box only (frozen C++ binaries + `mysql_proxy`, cap 1 GB; parity sessions only). Start/stop: `scripts/start_win.ps1` / `stop_win.ps1`; nightly backup: `scripts/backup_win.ps1` (retention 7).
- **Docs structure:** reorganized 2026-08-10 into `plans/`, `reference/`, `history/`, `guardrails/`, `decisions/`, `explanation/` + hubs (see `docs/README.md`); superseded plans moved to `history/` (read-only). Diátaxis modes (`tutorials/`, `how-to/`) have no content yet — created on demand.

## Verified numbers

| Item | Value | Evidence |
| --- | --- | --- |
| Workspace tests | **615 test attributes** (latest scan 2026-08-13 night; 607 at the 43rd-part count — per-crate breakdown then: protocol 81, network 28, database 96, game_core 161, server_realms 55, mysql_proxy 67, locale_import 19, bench_bot 34, quest_dsl 66). Orchestrator-verified runs 2026-08-13: **512** (42nd refactor) → **548** (shops/trade) → **564** (quest engine) → after dw_arrow: cargo check 4.52 s, **game_core 154 + server_realms 42 passed / 0 failed** → **565 passed / 0 failed / 35 ignored** (44th part) → **566 passed** (45th night) → **572 passed / 0 failed** (45th night follow-up; 615 attributes) → **573** (46th dawn; 616 attributes) → **647 passed / 0 failed** (49th part; 3 verifier runs); `replay_wal` PG 2/2 (2.34 s) | `#[test]`/`#[tokio::test]` scan in `source/reforge` (2026-08-13); CHANGELOG 39th–44th parts |
| `mysql_proxy` adapter | 67 tests (counted 2026-08-12; 53/53 at the G-PG gate 2026-08-10) — wire v10 codec, translate (§4 inventory as test table), session (per-slot search_path), config | `source/reforge/mysql_proxy/src/{wire,translate,session,config,server,sha1}.rs` |
| Parity (migration) | **30/30 tables** count+md5 equal (volatile `account.last_play` excluded — live-login write lands on PG only) | `scripts/gpg/parity_check.py` |
| Parity (boot) | A/B green — 0 new SYSERR + identical boot table lines vs the MariaDB baseline | `scripts/gpg/parity_boot.sh` |
| G-PG gate | Real client login on PostgreSQL: `LoginSuccess` 21:39:34 (core1 syslog); proxy log shows translated queries (`mysql_hash_password(...)`, `LOCALTIMESTAMP`/`EXTRACT(EPOCH ...)`) | `source/deploy/main/srv1/chan/ch1/core1/syslog`, `/tmp/gpg/proxy.log` (WSL) |
| F2a (Rust auth) | Real client login → character select through `server_realms --role auth` on PostgreSQL (hybrid stack, 2026-08-10) — auth log `login OK test key ...`, core1 `LoginSuccess` 00:03:42 | `source/deploy/main/srv1` auth on :30001, `/tmp/gpg/hybrid_auth.log` (WSL) |
| Graphs (graphify, at `b85a019`) | server **13,200 nodes / 33,251 links**; client **17,501 nodes / 39,258 links**; merged **30,701 nodes / 72,509 links** | `source/server/graphify-out/graph.json`, `source/client/graphify-out/graph.json`, `graphify-out/graph.json` |

## Current architecture decisions

1. **PostgreSQL as the one canonical database** — **IMPLEMENTED 2026-08-10**. ADR-0005 Accepted; gate closed (backlog B1–B8): PG 18.4 provisioned, phase-1 subset migrated (30 tables + 26 log DDL + `account.mysql_hash_password`), `mysql_proxy` adapter operating, parity A/B green, real client login on PG. MariaDB is the frozen migration/export source.
2. **Legacy packet compatibility stays isolated and deletable.** Compatibility structs/types are confined to a dedicated boundary so they can be removed wholesale when the legacy peers are gone — never woven into new code. **Accepted + implemented — [ADR-0006](decisions/0006-legacy-wire-pack-compat-boundary.md)** (`protocol::legacy` 151–153 in F2a 2026-08-10; deleted at F7).
3. **No partial Rust code embedded in the legacy client** during F0–F6. The Rust client ships standalone (Slint in F5→**deferred to F7** (review 2026-08-12), bevy in F7). **Accepted — [ADR-0007](decisions/0007-no-partial-rust-in-legacy-client.md).**
4. **Config format: TOML** — decided (ADR-0004); the proxy uses a minimal hand-rolled TOML subset (no config-rs — deferral); config-rs + clap enter at F2.
5. **Data layer driver + durability** — **ADR-0008 Accepted 2026-08-11**: tokio-postgres 0.7 as the `database` driver (sqlx not adopted — WAL phase done 2026-08-12); PostgreSQL-only repos, no direct-sql backend; durable = **save-by-event** (event → Batcher ≤100 ms → local WAL → PG, replay idempotent — implemented 2026-08-12, F3 phase 2); **the pool clause is EXECUTED 2026-08-13 (44th part)** — deadpool-postgres 0.14.1 (same driver, tokio-postgres 0.7.9), `PgPool` shared at channel level, one `Batcher` per channel, `pool_max_size` default 10 (ADR-0008 note); RLS post-WAL; Patroni F5/F6.
6. **Server-side locale** — **ADR-0009 Accepted (2026-08-12)**: the server owns all text per player language (one `common.*` table per domain, `CG_LOCALE_REQUEST`/`GC_LOCALE`, EN fallback); importer live (`locale_import`, 2026-08-12), wire slice pending.
7. **Domain boundaries + ECS** — **ADR-0010 Accepted (2026-08-12)**: pure-function domain modules + **bevy_ecs World** (user decision — mob density is the core requirement) + per-connection session state + WorldStore; translator-vs-core boundary; wire debt D1–D6 (F7 deletion list). **Migration slice DONE 2026-08-13 (39th part):** `MobCache → World` landed (`game_core/src/ecs.rs`, split 42nd part into `ecs/` with `Intent`/`NpcEvent` wrapper sub-enums); **spawn dinámico DONE 2026-08-13 (40th part):** channel-level shared World with `spawn_despawn_system` (SPAWN_VIEW 2500 / DESPAWN_RADIUS 4000, `ecs/systems/spawn.rs:22,26`) + `Intent::Join` mpsc (`ecs/events.rs:303-307`). **ECS executor correction (2026-08-13):** bevy_ecs runs with only `features = ["std"]` (no `multi_threaded` → no `bevy_tasks`), so `Schedule::default()` is a single-threaded executor and the 5 systems are `.chain()`ed in parity order (`game_core/src/ecs/world.rs:83-93`); the "one-line `multi_threaded` toggle" does not exist — parallel ECS is a pending decision (ADR-0010 §1).
8. **Anti-hack** — **ADR-0011 Accepted (2026-08-12)**: always-on controls ratified (speedhack/teleport/unknown-header/timeout/server-clock cooldowns), signed clock wrap decided, pending controls phased (walkability + speed envelope, rate limits, buffs, item-ACID, farm bots).
9. **Runtime hosting** — **ADR-0012 Accepted (2026-08-12), executed + verified**: the whole runtime is **native Windows** (PG 18.4 service + Rust auth/channel from `source/deploy/win`); WSL shrinks to an on-demand oracle box for the frozen C++ binaries until F6 (side-by-side), then `wsl --unregister Debian-M2`. The C++ server is NEVER rebuilt (user decision).
10. **Client rewrite** — **ADR-0013 Accepted (2026-08-13)**: Bevy 0.19 + slint UI + build-time legacy asset conversion (decisions D1–D14); canonical plan `plans/client-rewrite.md` (Draft v0.1).

## Status by phase

| Phase | Status | Next action |
| --- | --- | --- |
| **F0 — Foundations** | `protocol` crate complete — **30/30 at F0 close** (81 test attributes incl. golden/datachannel/world by 2026-08-12); F0 milestone (byte-exact LOGIN3) **MET 2026-08-11** (golden capture `auth_login3_40999.bin`, 88B) | F0 closed; pending: GitHub repository preparation (sources only) |
| **F1 — Network** | Listener, framer, handshake done — **25/25 tests**; **F1.6 MET** (f16_peer ↔ live auth, no floods, 2026-08-10) | F1 milestone closed; remaining debt: retry-on-wrong-nonce rationale, partial-echo test |
| **G-PG — PostgreSQL cutover** | **COMPLETE (2026-08-10)** — ADR-0005 Accepted, B1–B8 done, real login on PG, parity A/B green | Close-out only: sync WSL/Windows source copies; optional graph refresh |
| **F2 — Auth + first client batch** | **F2a + F2b DONE (2026-08-11)** — Rust auth + client version/hwid batch, verified end-to-end with the real client (world entry) | **F3** (data layer + data channel) |
| **F3+ — Data, world, parity** | **F3 done + F4 MILESTONE MET (2026-08-11)** — the REAL client enters the world against the Rust core and stays (select → DirectEnter → loading → map 41, 50+ s sustained; world empty — NPCs F5). World repos + WAL (phase 2 done 2026-08-12) + snapshot harness + data channel 162+ + client instrumentation (`python_error.log`) + **bevy_ecs World with spawn dinámico (ECS migration + shared World DONE 2026-08-13)** + **MapStore walkability (map.rs)** + **ACID units (`Batcher::flush` + `exchange_mutated`)** | **F5 (gameplay)**: F5.3 slices 1–17 + ECS + spawn dinámico + walkability + benchmark harness done (2026-08-13, below); pending: movement envelope follow-ups, quest pending actions (say_reward, send_letter, set_quest_state, target_vid, affect_*, input_number), auction/unified trade, GM extensions (mob spawn, locale), family extraction, harness walk, benchmark ladder 250/500/1000 (step 1 done), per-tick CPU measurement (real-client smoke DONE — 45th part); **pool EXECUTED + ladder step 1 DONE (44th part)** — deploy verified 18:01:39 (binary 4,509,184 B, SHA256 `77D8ACD2…C732`); redeploy waves 6–7 CLOSED; **wave-45 session verified** (deploy 22:06 `2A71E57E`); **night follow-up DONE** (quests wired — 194/194 corpus, 106 with Chat triggers; shops linked to visible NPCs; vision 8000 / despawn 10000; mob move duration real; deploy 00:15 `8C335FAA`; remaining: converter ~88, skills GAPs, GM mob spawn; `LoginEarly` covered by the wave-46 leniency); **wave-46 dawn DONE** (quest-boot A/B fix — corpus in background; handshake leniency 45 s; close panic fix; channel.inf → 0; deploy 02:11 `015FB634` — current stack auth.021225/channel.021225; 573 tests; in diagnosis: shop opening, quest text resolution); F4 tail: client UTF-8 name overrides (API done, wire pending) |

**F5.3 gameplay slices — 17 done (2026-08-12, direct orchestrator implementation; per-crate `cargo test` green + clippy clean):** s1 kill rewards + chat + client locale cache; s2 item drops + pickup; s3 NPC AI aggro + chase + `GC_MOVE`; s4 mob attack in range; s5 PC death + revive; s6 warp-to-city + de-aggro; s7 idle patrol; s8 stacking pickup; s9 player DEF in mob damage; s10 proactive aggro + `aggressive_sight`; s11 potions + latent framer fix; s12 item move/stack/split; s13 equip/unequip; s14 equipped items affect combat; s15 ComputeParts; s16 FindEquipCell; s17 weapon attack_speed. **Runtime migration executed (ADR-0012, 38th part): all-Windows native stack verified with the real client — login → select → world → movement; mobs spawn (SPAWN_VIEW 2500, 11 visible); "0 items" = not a bug (22 rows, owner_id 2).** **5-front wave (2026-08-13, 39th part): ECS migration DONE — the world state is a bevy_ecs World (`game_core/src/ecs.rs`, 986 lines; components/resources/systems + `WorldSim`; channel.rs → World; workspace 359 passed / 0 failed; deviations documented: single-threaded ECS schedule (the `multi_threaded` toggle does not exist — ADR-0010 §1), SpawnCache as resource, World per-connection → channel-level shared since the 40th part); WAL idempotency DONE (both INSERT paths `ON CONFLICT DO NOTHING`; `replay_wal` PG test un-gated 2/2 in 2.34 s); dwLoginKey real flow DONE (auth); client UTF-8 override API DONE (exe 5,130,752 B); benchmark harness DONE (`bench_bot` crate 27 attrs + `bench_capture`/`--bench-capture`).** **Wave 2 (2026-08-13, 40th part): spawn dinámico DONE — shared World (`spawn_despawn_system`, SPAWN_VIEW 2500 / DESPAWN_RADIUS 4000, `Intent::Join` mpsc, bench_capture wired 4/4); walkability + speed envelope DONE (`game_core/src/map.rs` 553 lines — map 41 decoded: 2,176,848 blocked + 1,076,378 water cells; envelope = speed×(dt+100ms)/1000×1.20, `ExceedsEnvelope` movement.rs:53-67; CG_MOVE validates BEFORE `process_move`); quest DSL core DONE (`quest_dsl` crate, 13 tests; qc→DSL converter IN PROGRESS); F3 tail ACID DONE (`Batcher::flush` wal.rs:398, `exchange_mutated` one tx item.rs:233, GuildRepo, `checked_gold_mutation` economy.rs:97 — database 95 attrs; gaps documented: guild_member/grade/comment not migrated, `CHECK gold>=0` not in PG); workspace 418 passed / 0 failed; binary 3,818,496 B live.** **Wave 3 (2026-08-13, 41st part): skills + server-timed buffs DONE (`game_core/src/skill.rs` 732 lines/15 tests; ecs `Mp`/`SkillLevels`/`SkillCooldowns`/`Affects` + `SkillTable` + `process_skill` + `affects_system`; `CG_USE_SKILL` 52/9 B + `GC_AFFECT_ADD`/`GC_AFFECT_REMOVE` — 481 passed / 0 failed; binary 3,935,232 B live; GAPs: SPLASH/PARTY/HORSE, `skill_power.txt`, buff numeric application, quest-granted skills); qc→DSL converter DONE (`quest_dsl` 66 tests incl. similarity engine; corpus 194/194 ~2 s; 5,513 unmapped → Rust modules; 6 family proposals 112/194 files; family extraction IN PROGRESS); benchmark FIRST LOAD SIGNAL (`logs/bench-run-2026-08-13.md`: 20/20 OK, 0 failures, 0 panics; world_ms median 7829 @20 bots; FINDING: spawn materialization reaches only first-comers — fix IN PROGRESS; envelope rejects harness walk — harness fix).** **Wave 7 (2026-08-13, 43rd part): NPC shops + player trade DONE (`shop.rs` 406 lines buy/sell parity + `trade.rs` 370 lines TradeSession one-tx commit via `exchange_mutated`/`Batcher::flush`; GC_SHOP 1888 B / GC_EXCHANGE 47 B wire; workspace 548 passed); quest runtime engine DONE (`quest/engine.rs` 859 lines — `{quest}.__status` 1-based, wait/select suspension, player.quest persistence; pending actions: say_reward/send_letter/set_quest_state/target_vid/affect_*/input_number; 564 passed); GM subset DONE (`gm.rs` + channel/gm.rs — warp/item/notice/level; mob spawn + locale pending); dw_arrow DONE (arrow gate + `pending_arrow_shot` + real arrow count in ADDITIONAL_INFO — packets.rs:311-333; cargo check 4.52 s, game_core 154 + server_realms 42 passed).** **IN PROGRESS: spawn-concurrency fix, family extraction, harness walk, benchmark ladder; NEXT: ladder steps 250/500/1000, per-tick CPU measurement, real-client smoke (deploy of waves 6–7 + pool DONE 18:01:39, 44th part), quest pending actions, auction/unified trade, data channel 162/163, locale wire slice, manifest/hot reload, auto-ban.** Slint → F7, REST → post-cutover.

## Next gates

1. **F3 milestone (redefined 2026-08-12)**: port the remaining QIDs to the `database` crate (items, social), activate the pull data channel (162/163) end-to-end, maintain `PROTO_FROM_DB`. WAL phase 2 (durable-first + idempotent replay) is **DONE (2026-08-12, 11th part)**; gated `replay_wal` PG test to un-gate BEFORE trade/safebox.
2. **F5 gameplay tail (2026-08-13, 41st–44th parts)**: **ECS migration DONE**; **spawn dinámico DONE** (channel-level shared World; **spawn-concurrency FINDING — RESOLVED (44th part: 11/11 spawns on all 20 bots)**); **provisional N-bot benchmark — FIRST LOAD SIGNAL DONE** (`logs/bench-run-2026-08-13.md`: 20/20 OK, 0 failures, 0 panics, world_ms median 7829 @20 bots, ~13 % spread) **+ LADDER STEP 1 DONE (44th part)** (`logs/bench-run-2026-08-13-wave44.md`: 100/100 OK, world_ms median 2,742, spawns 1,100 = 11/bot, AI-tick <1 ms; 20-bot rerun: world_ms 1,052 vs 7,829 baseline, spawns 11/11 on all bots; remaining: 250/500/1000, per-tick CPU (real-client smoke DONE — wave 45: session complete)); **the 2 non-idempotent WAL paths DONE** (`replay_wal` PG test UN-GATED 2/2 green); **walkability + speed envelope DONE** (map.rs + movement.rs — P0 hole closed; envelope rejects the 300 u/s harness walk — harness fix in progress); **quest DSL core + converter DONE** (`quest_dsl` 66 tests incl. similarity engine; corpus 194/194; family extraction IN PROGRESS); **skills + server-timed buffs DONE** (41st part; GAPs: SPLASH/PARTY/HORSE, `skill_power.txt`, buff numeric application, quest-granted); **dw_arrow DONE + shops/trade DONE + quest runtime engine DONE + GM subset DONE** (43rd part — wave 7; quest pending actions, auction, GM extensions remain); **wave-44 deploy DONE + verified 18:01:39** (binary 4,509,184 B, SHA256 `77D8ACD2…C732`, auth.180138/channel.180138 — waves 6–7 + pool + metrics ship in it; redeploy of parts 42–43 CLOSED); **pool (deadpool-postgres) EXECUTED** (ADR-0008 — channel-level `PgPool`, one `Batcher` per channel, `pool_max_size` default 10; 565 passed / 0 failed / 35 ignored); **wave-45 night: real-client session COMPLETE** (subtype/walkability/envelope fixes, deploy 22:06 `2A71E57E`; spawns reverted + tanacas removed per user directive — no game-data edits without explicit confirmation) **+ night follow-up** (quests wired — 194/194 corpus, 106 with Chat triggers; shops linked to visible NPCs; vision 8000/despawn 10000; mob move duration real; deploy 00:15 `8C335FAA`; 572 passed; remaining: converter ~88, skills GAPs, GM mob spawn) **+ wave-46 dawn** (quest-boot A/B fix — corpus load in background; handshake leniency 45 s — the silent-handshake/LoginEarly case covered; close panic fix; channel.inf → 0; deploy 02:11 `015FB634` — current stack auth.021225/channel.021225; 573 passed; in diagnosis: shop opening, quest text resolution); **real-client E2E smoke every N slices**; then full scale benchmark, Slint standalone (→ F7), REST/metrics (→ post-cutover).
3. **F2a debt — CLOSED 2026-08-13 (39th part)**: `dwLoginKey` real flow done at the auth (real `LoginKeyStore`, LOGIN_BY_KEY via LOGIN3 `passwd[17]`, no wire change, 18 auth unit tests). Channel-side LOGIN2 consumption stays for the channel lane.
4. **F4 tail**: client UTF-8 name overrides — override **API DONE 2026-08-13** (fix-1: `SetLocaleName`/`SetItemLocaleName` + Python hooks, exe 5,130,752 B deployed; the server does not send overrides yet — data channel future); minimal Entity core + ECS — **DONE 2026-08-13** (migration landed, 39th part).
5. **Capture harness** (F0 debt): LOGIN3 golden fixture DONE 2026-08-11 (`auth_login3_40999.bin` 88B); extend to more packets.
6. **NPC motion data** (pre-existing data gap, not PG): `mob_proto.folder=''` for the 20000+ custom NPCs (1144 races) — partially fixed 2026-08-11 (9 races mapped to existing folders); remaining folder audit.

## Where to look next

- Full plan with acceptance criteria per task: [`../ROADMAP.md`](../ROADMAP.md)
- **Consolidated master plan (oracle-reviewed 2026-08-12):** [`plans/master-plan.md`](plans/master-plan.md)
- Architecture decisions: [`decisions/`](decisions/) (0001 PostgreSQL, 0002 game+db unification, 0003 workspace, 0004 names/config, 0005 cutover — Accepted+implemented, 0006 compat boundary, 0007 client boundary, 0008 data layer, 0009 locale, 0010 ECS, 0011 anti-hack, 0012 runtime hosting, **0013 client rewrite — Accepted 2026-08-13**)
- Design reference: [`plans/server-rewrite.md`](plans/server-rewrite.md)
- Wire contract: [`reference/protocol/login-flow.md`](reference/protocol/login-flow.md)
- Legacy compatibility boundary: [`reference/protocol/legacy-compatibility.md`](reference/protocol/legacy-compatibility.md)
- **G-PG database inventories:** [`reference/database/legacy-schema.md`](reference/database/legacy-schema.md) · [`reference/database/legacy-sql-compatibility.md`](reference/database/legacy-sql-compatibility.md)
- **G-PG runbook + harness:** [`scripts/gpg/`](../scripts/gpg/) — install/provision chain (02–09), `parity_check.py`, `parity_boot.sh`, `start_pg_stack.sh`
- Agent team model (roster, hierarchy, session rules): [`explanation/agent-organization.md`](explanation/agent-organization.md)
- Superseded plans/specs (read-only): [`history/`](history/)
- Change history: [`../CHANGELOG.md`](../CHANGELOG.md)
