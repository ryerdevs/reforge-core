# Metin2 Skills Design Addendum

The project skills setup also includes a root `AGENTS.md` describing the Alpine Linux and Docker compatibility baseline, the future Rust rewrite, and the long-term plan to unify the current `game` and `db` responsibilities behind a coherent server architecture.
