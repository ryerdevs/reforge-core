[package]
name = "admin_tui"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Operator TUI panel for the reforge-core deploy: start/stop, logs, backup, restore, config."

[lints]
workspace = true

[dependencies]
ratatui = "0.30"
crossterm = "0.28"